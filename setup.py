from setuptools import setup, find_packages

setup(
    name='dpm_functions',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # dependencias, ejemplo:
        'numpy'
        # etc.
    ]
)
